<%= form_with(model: [@pr_item.pr_list, @pr_item]) do |f| %>
  <% if @pr_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pr_item.errors.count, "error") %> prohibited this pr_item from being saved:</h2>

      <ul>
      <% @pr_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :completed %><br />
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :completed_at %><br />
    <%= f.datetime_select :completed_at %>
  </div>
  <div class="field">
    <%= f.label :pr_list %><br />
    <%= f.text_field :pr_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
