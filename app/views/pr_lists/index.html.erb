

<div class="container">
  <div class="row">
    <div class="col-md-10 col-lg-8 offset-md-1 offset-lg-2">
      <div class="card mt-5">



<div class="card-header d-flex justify-content-between">
  <span>
   <h4 style=" font-family: 'Montserrat', sans-serif;"><u color="cyan">Projects List</u></h4>
  </span>
  <%= link_to 'New Project', new_pr_list_path, class: 'btn btn-primary btn-sm' %>
</div>



<ul class="list-group">
  <% @pr_lists.each do |pr_list| %>
  <% if pr_list.user == current_user %>
  <li class="list-group-item">
    <%= link_to pr_list.title, pr_list %>
  </li>
    <% end %>
    <% end %>
</ul>






      </div>
    </div>
<div class="col-md-10 col-lg-8 offset-md-1 offset-lg-2">
  <div class="card mt-5">   

<%= render "pr_lists/timeline" %>
</div>
</div>
</div>
</div>

 <!--

<h1>Pr Lists</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pr_lists.each do |pr_list| %>
      <tr>
        <td><%= pr_list.title %></td>
        <td><%= link_to 'Show', pr_list %></td>
        <td><%= link_to 'Edit', edit_pr_list_path(pr_list) %></td>
        <td><%= link_to 'Destroy', pr_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pr List', new_pr_list_path %>

-->