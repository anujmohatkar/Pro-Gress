<%= form_with(model: pr_list) do |form| %>
  <% if pr_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_list.errors.count, "error") %> prohibited this pr_list from being saved:</h2>

      <ul>
        <% pr_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
